name: Frontend CD

on:
  workflow_run:
    workflows: ["Frontend CI"]
    types:
      - completed
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}

      - name: Prepare deployment package
        run: |
          # Create deployment directory
          mkdir -p deploy

          # Copy server build output
          cp -r .output/server deploy/
          cp -r .output/public deploy/

          # Create production package.json
          cat > deploy/package.json << EOF
          {
            "name": "phoenix-orders-frontend",
            "version": "1.0.0",
            "type": "module",
            "scripts": {
              "start": "node server/index.mjs"
            },
            "engines": {
              "node": "20.x"
            }
          }
          EOF

          # Create Procfile for Elastic Beanstalk
          echo "web: npm start" > deploy/Procfile

          # List deployment contents
          echo "=== Deployment package contents ==="
          ls -la deploy/

      - name: Generate deployment package
        run: |
          cd deploy
          zip -r ../deploy.zip .
          cd ..
          ls -lh deploy.zip

      - name: Deploy to Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v22
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          application_name: ${{ secrets.EB_APPLICATION_NAME }}
          environment_name: ${{ secrets.EB_ENVIRONMENT_NAME }}
          version_label: ${{ github.sha }}
          region: ${{ secrets.AWS_REGION }}
          deployment_package: deploy.zip

      - name: Deployment successful
        run: |
          echo "✅ Frontend deployed successfully to Elastic Beanstalk!"
          echo "URL: http://Phoenixorders-dev.us-east-1.elasticbeanstalk.com"
