name: Frontend CD

on:
  workflow_run:
    workflows: ["Frontend CI"]
    types:
      - completed
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      actions: read

    env:
      TF_VAR_frontend_image: "ghcr.io/${{ github.repository_owner }}/modulo3-frontend:latest"
      TF_VAR_backend_url: ${{ secrets.BACKEND_URL }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # ─────────────────────────────────────────
      # PASO 1: Configurar credenciales AWS
      # Terraform necesita acceso a AWS para crear recursos
      # ─────────────────────────────────────────
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # ─────────────────────────────────────────
      # PASO 2: Instalar Terraform
      # ─────────────────────────────────────────
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"

      # ─────────────────────────────────────────
      # PASO 3: Terraform Init
      # Descarga los plugins necesarios (proveedor AWS)
      # ─────────────────────────────────────────
      - name: Terraform Init
        working-directory: ./terraform
        run: terraform init

      # ─────────────────────────────────────────
      # PASO 4: Terraform Plan
      # Muestra qué recursos va a crear/modificar (sin crear aún)
      # ─────────────────────────────────────────
      - name: Terraform Plan
        working-directory: ./terraform
        run: terraform plan

      # ─────────────────────────────────────────
      # PASO 5: Terraform Apply
      # CREA LA INFRAESTRUCTURA DEL FRONTEND AUTOMATICAMENTE
      # Si ya existe, la verifica y actualiza si es necesario
      # ─────────────────────────────────────────
      - name: Terraform Apply (Provisionar servidor frontend)
        working-directory: ./terraform
        run: terraform apply -auto-approve

      # ─────────────────────────────────────────
      # PASO 6: Obtener URL del frontend desde Terraform
      # No necesitas copiar nada manualmente - es automático
      # ─────────────────────────────────────────
      - name: Get Terraform outputs
        working-directory: ./terraform
        id: tf_outputs
        run: |
          echo "frontend_url=$(terraform output -raw frontend_url)" >> $GITHUB_OUTPUT
          echo "eb_env_name=$(terraform output -raw eb_environment_name)" >> $GITHUB_OUTPUT

      # ─────────────────────────────────────────
      # PASO 7: Crear Dockerrun.aws.json
      # Le dice a Elastic Beanstalk qué imagen Docker usar
      # ─────────────────────────────────────────
      - name: Prepare Docker deployment package
        run: |
          SHORT_SHA=$(echo "${{ github.event.workflow_run.head_sha }}" | cut -c1-7)
          IMAGE="ghcr.io/${{ github.repository_owner }}/modulo3-frontend:sha-${SHORT_SHA}"
          echo "Deploying image: $IMAGE"

          cat > Dockerrun.aws.json << EOF
          {
            "AWSEBDockerrunVersion": "1",
            "Image": {
              "Name": "${IMAGE}",
              "Update": "true"
            },
            "Ports": [
              {
                "ContainerPort": "8080",
                "HostPort": "80"
              }
            ]
          }
          EOF

          cat Dockerrun.aws.json
          zip deploy.zip Dockerrun.aws.json

      # ─────────────────────────────────────────
      # PASO 8: Deploy a Elastic Beanstalk
      # EB descargará la imagen de ghcr.io y la correrá
      # ─────────────────────────────────────────
      - name: Deploy to Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v22
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          application_name: ${{ secrets.EB_APPLICATION_NAME }}
          environment_name: ${{ secrets.EB_ENVIRONMENT_NAME }}
          version_label: docker-${{ github.event.workflow_run.head_sha }}-${{ github.run_number }}
          region: ${{ secrets.AWS_REGION }}
          deployment_package: deploy.zip
          use_existing_version_if_available: true

      # ─────────────────────────────────────────
      # PASO 9: Smoke Test
      # Usa la URL generada por Terraform automáticamente
      # ─────────────────────────────────────────
      - name: Smoke test deployed environment
        run: |
          URL="${{ steps.tf_outputs.outputs.frontend_url }}"
          echo "Waiting for the frontend to respond at $URL ..."
          for i in $(seq 1 15); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$URL" || echo "000")
            if [ "$STATUS" = "200" ]; then
              echo "✅ Smoke test passed (HTTP $STATUS)"
              exit 0
            fi
            echo "Attempt $i: got HTTP $STATUS, retrying in 10s..."
            sleep 10
          done
          echo "❌ Smoke test failed: frontend did not return HTTP 200"
          exit 1

      - name: Deployment successful
        run: |
          echo "✅ Frontend deployed via Docker image con IaC!"
          echo "Image: ghcr.io/${{ github.repository_owner }}/modulo3-frontend:latest"
          echo "URL: ${{ steps.tf_outputs.outputs.frontend_url }}"
